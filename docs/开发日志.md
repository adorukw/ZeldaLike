# ZeldaLike 开发日志

## 2026-07-19

### 今日任务

- [x] 搭建项目框架

### 任务概述

1. 编写了核心文件
2. 编写了CMakeLists.txt

### 问题及解决

1. **问题**：zed编辑器默认用cland做LSP，不识别`#include "game.h"`这种写法  
   **解决**：在`CMakeLists.txt`中添加
   ```
   add_custom_target(copy_compile_db ALL
       COMMAND ${CMAKE_COMMAND} -E copy_if_different
           ${CMAKE_BINARY_DIR}/compile_commands.json
           ${PROJECT_SOURCE_DIR}/compile_commands.json
   ```
   会自动在项目根目录生成`compile_commands.json`文件给cland识别

### 未来计划
